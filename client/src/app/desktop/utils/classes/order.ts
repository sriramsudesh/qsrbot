export class OrderedItem {
  constructor(
    public index: number,
    public item: string,
    public type: string,
    public size: string,
    public options: string,
    public cost: number
  ) {
    // Lett blank
  }
}
